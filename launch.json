{
    
    "version": "0.2.0",
    "configurations":[
        {
            "type":"chrome",
            "request": "launch",
            "name":"launch chrome against localhost",
            "file":"${workspaceFolder}/kokki.html",
            "webRoot": "${workspaceFolder}"
        }
    ]
}